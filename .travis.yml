language: julia
os:
  - linux
  - osx
julia:
  - 1.0.0

notifications:
  email: false
script:
  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
  - julia -e 'using Pkg; pkg"build QuantumInformation"; pkg"add StatsBase"; pkg"add Test"; pkg"test QuantumInformation --coverage"'
  - julia -e 'using Pkg; pkg"add Distributions"';
  - julia --code-coverage randommatrices/test/runtests.jl

after_success:
  - julia -e 'import QuantumInformation; using Pkg; pkg"add Coverage"; using Coverage; Coveralls.submit(vcat(process_folder(), process_folder("randommatrices/src")))'
  - julia -e 'using Pkg; pkg"add Documenter"'
  - julia -e 'import QuantumInformation; cd(joinpath(dirname(pathof(QuantumInformation)), "..")); include(joinpath("docs", "make.jl"))'
